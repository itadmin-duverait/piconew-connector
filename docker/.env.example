# IP address of this machine on your local network
# Find it with: ip addr (Linux/Pi) or ipconfig (Windows) or ifconfig (Mac)
CONNECTOR_IP=192.168.4.1

# PicoNew cloud URL — do not change
PICONEW_API_URL=https://piconew.com
